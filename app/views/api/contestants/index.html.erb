<h1>Listing api_contestants</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @api_contestants.each do |api_contestant| %>
      <tr>
        <td><%= link_to 'Show', api_contestant %></td>
        <td><%= link_to 'Edit', edit_api_contestant_path(api_contestant) %></td>
        <td><%= link_to 'Destroy', api_contestant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contestant', new_api_contestant_path %>
