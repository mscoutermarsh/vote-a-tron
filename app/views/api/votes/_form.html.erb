<%= form_for(@api_vote) do |f| %>
  <% if @api_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_vote.errors.count, "error") %> prohibited this api_vote from being saved:</h2>

      <ul>
      <% @api_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
